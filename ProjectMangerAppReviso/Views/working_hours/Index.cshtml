@model IEnumerable<ProjectMangerAppReviso.Models.working_hours>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.active_projects.pName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.startDT)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.endDT)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.active_projects.pName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.startDT)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.endDT)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
